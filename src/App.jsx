import { useState } from "react";
import Navbar from "./components/Navbar";

function App() {
  return (
    <div className="">
      <Navbar />
      <section id="home" className="h-screen bg-red-200 w-full">
        tes
      </section>
      <section id="schedule" className="h-screen bg-green-200 w-full">
        tes
      </section>
      <section id="teacher" className="h-screen bg-gray-200 w-full">
        tes
      </section>
    </div>
  );
}

export default App;
